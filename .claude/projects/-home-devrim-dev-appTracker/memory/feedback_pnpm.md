---
name: Use pnpm for frontend
description: User uses pnpm instead of npm for the frontend
type: feedback
---

Use pnpm (not npm) for all frontend package management commands in this project.

**Why:** User preference — they use pnpm.
**How to apply:** Any time running or suggesting frontend install/run commands, use `pnpm install`, `pnpm dev`, `pnpm build`, etc.
